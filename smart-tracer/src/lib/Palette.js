import { createMuiTheme } from '@material-ui/core/styles';

const palette = createMuiTheme({
  palette: {
    colors: {
        main: '#fff',    
        secondary: '#CC0941',
        darkSecondary:'#B10839',
        default: '#F3F3F3',
        darkDefault:'#E1E1E1',
        black: '#000'
    },
  },
});

export default palette;